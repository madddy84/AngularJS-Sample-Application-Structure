
module SampleWeb.Application1.Services.Tests {
    describe("Tests to assert state management service", () => {
        beforeEach(() => {
            angular.mock.module("application1Services");
        });

        beforeEach(angular.mock.inject(($window, $rootScope, $q) => {

        }));

        it("Test: Scenario 1", angular.mock.inject(() => {

        }));

    });
}